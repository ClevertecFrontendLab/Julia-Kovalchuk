export enum ROUTE {
  HOME = '/',
  OFFER = '/offer',
  RULES = '/rules',
  BOOKS = '/books',
  ALLBOOKS = '/all',
  CATEGORY = '/books/:category',
  PROFILE = '/profile',
  DETAILS = 'books/all/',
}
